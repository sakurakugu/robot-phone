{
  "extends": "@react-native/typescript-config",
  "compilerOptions": { // 这个保存是react-native内部的es2020.data，不用管他
    "types": ["jest"]
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "exclude": ["**/node_modules", "**/Pods"]
}
